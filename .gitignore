cache/
*.pyc
__pycache__/
.pytest_cache/
.DS_Store
tsconfig.tsbuildinfo
data/store.db
data/backtest_report.txt
data/backtest_results.json
data/lseg_equity_prices
data/agency_ratings.csv
data/market_features.csv
data/cases.csv
# Trained migration model (generated by scripts.seed_demo / src.model.train). The
# deployed app reads predictions from Supabase, not this file; the walk-forward
# vintages (data/model_vintages/) and scorecard (data/migration_eval.json) stay
# tracked so the /backtest page can read them from the repo.
data/migration_model.joblib

# Licensed LSEG data drops (raw rating history + universe crosswalk) — not ours
# to redistribute. Consumed offline by scripts/seed_agency_ratings.py; keep local.
data/ratings_history_raw.csv
data/ratings_history.csv
data/distress_events.csv
data/agency_ratings.csv.bak
data/universe_xref.csv

# LSEG data pull request (targeted issuer/ticker/RIC/permid list for the data team)
# — contains licensed identifiers; keep local, never commit.
data/lseg_pull_request.csv
.env
.env.local

# Next.js
.next/
node_modules/

# Vercel
.vercel/

# Supabase CLI local state (auto-generated; never commit)
supabase/.temp/

# Docs
docs/

# Run logs (regenerated per run; not part of the program)
logs/